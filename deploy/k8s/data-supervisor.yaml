apiVersion: apps/v1
kind: Deployment
metadata:
  name: aeitron-data-supervisor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aeitron-data-supervisor
  template:
    metadata:
      labels:
        app: aeitron-data-supervisor
    spec:
      containers:
        - name: supervisor
          image: aeitron-api:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "src.aeitron.learning.supervisor"]
          env:
            - name: AEITRON_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: database-url
            - name: AEITRON_DATA_SOURCES
              value: config/data_sources.ultimate.json
            - name: AEITRON_RAW_OUTPUT_DIR
              value: /data/raw
            - name: AEITRON_CLEAN_OUTPUT_DIR
              value: /data/clean
            - name: AEITRON_OBJECT_STORE_URI
              value: s3://aeitron-datasets/pretraining
            - name: AEITRON_WORKER_REPLICAS
              value: "4"
            - name: AEITRON_ASYNC_WORKERS
              value: "32"
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: minio-root-user
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: minio-root-password
            - name: AWS_EC2_METADATA_DISABLED
              value: "true"
          volumeMounts:
            - name: data-artifacts
              mountPath: /data
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "4"
              memory: 8Gi
      volumes:
        - name: data-artifacts
          persistentVolumeClaim:
            claimName: aeitron-data-artifacts

