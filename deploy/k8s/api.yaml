apiVersion: apps/v1
kind: Deployment
metadata:
  name: aeitron-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aeitron-api
  template:
    metadata:
      labels:
        app: aeitron-api
    spec:
      containers:
        - name: api
          image: aeitron-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8090
          env:
            - name: AEITRON_AUTH_ENABLED
              value: "1"
            - name: AEITRON_QUOTA_ENABLED
              value: "1"
            - name: AEITRON_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: jwt-secret
            - name: AEITRON_ALLOW_TOKEN_ISSUE
              value: "0"
            - name: AEITRON_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: database-url
            - name: AEITRON_REDIS_URL
              value: redis://aeitron-redis:6379/0
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/ready
              port: 8090
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "500m"
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
---
apiVersion: v1
kind: Service
metadata:
  name: aeitron-api
spec:
  selector:
    app: aeitron-api
  ports:
    - port: 80
      targetPort: 8090

