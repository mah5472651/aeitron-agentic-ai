apiVersion: apps/v1
kind: Deployment
metadata:
  name: aeitron-data-worker
spec:
  replicas: 4
  selector:
    matchLabels:
      app: aeitron-data-worker
  template:
    metadata:
      labels:
        app: aeitron-data-worker
    spec:
      containers:
        - name: crawler
          image: aeitron-api:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "src.aeitron.learning.worker"]
          env:
            - name: AEITRON_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: database-url
            - name: AEITRON_DATA_SOURCES
              value: config/data_sources.ultimate.json
            - name: AEITRON_RAW_OUTPUT_DIR
              value: /data/raw
            - name: AEITRON_CLEAN_OUTPUT_DIR
              value: /data/clean
            - name: AEITRON_ASYNC_WORKERS
              value: "32"
            - name: AEITRON_BATCH_DOCS
              value: "10000"
            - name: AEITRON_WORKER_LOOPS
              value: "100"
          volumeMounts:
            - name: data-artifacts
              mountPath: /data
          resources:
            requests:
              cpu: "1"
              memory: 1Gi
            limits:
              cpu: "4"
              memory: 4Gi
      volumes:
        - name: data-artifacts
          persistentVolumeClaim:
            claimName: aeitron-data-artifacts
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aeitron-data-artifacts
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Ti

