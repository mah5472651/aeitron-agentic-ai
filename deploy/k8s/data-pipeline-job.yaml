apiVersion: batch/v1
kind: Job
metadata:
  name: aeitron-data-pipeline
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: pipeline
          image: aeitron-api:latest
          imagePullPolicy: IfNotPresent
          command:
            - python
            - -m
            - src.aeitron.learning.data_pipeline
          args:
            - --sources
            - config/data_sources.ultimate.json
            - --dataset-id
            - aeitron-defensive-coding-corpus
            - --work-dir
            - /data/pipeline
            - --frontier-backend
            - postgres
            - --postgres-dsn
            - "$(AEITRON_DATABASE_URL)"
            - --object-store-uri
            - s3://aeitron-datasets/pretraining
            - --object-store-endpoint-url
            - http://aeitron-minio:9000
            - --max-docs
            - "1000000"
            - --workers
            - "64"
            - --max-depth
            - "2"
            - --vocab-size
            - "64000"
            - --sequence-length
            - "2048"
            - --shard-token-count
            - "1000000"
            - --skip-train
          env:
            - name: AEITRON_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: database-url
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: minio-root-user
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aeitron-secrets
                  key: minio-root-password
            - name: AWS_EC2_METADATA_DISABLED
              value: "true"
          volumeMounts:
            - name: data-artifacts
              mountPath: /data
          resources:
            requests:
              cpu: "4"
              memory: 16Gi
            limits:
              cpu: "16"
              memory: 64Gi
      volumes:
        - name: data-artifacts
          persistentVolumeClaim:
            claimName: aeitron-data-artifacts

